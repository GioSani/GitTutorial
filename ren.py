import numpy as np
import pandas as pd
import time
#np.random.seed(seed=int(time.time()))
nd_reinsurance_list=np.random.normal(loc=45, scale=4, size=1)

def ren():
    #n=0
    #k=1
    #while n<10:
        #np.random.seed(k)

    nd_reinsurance_list=np.random.normal(loc=45, scale=4, size=1)
    print nd_reinsurance_list
    #n+=1
k=0
while k<10:

    print ren()
    k=k+1